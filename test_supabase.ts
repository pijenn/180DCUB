import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!); async function test() { const { data, error } = await supabase.from('transaction_items').select('price_at_buy, products!inner(name, type, file_url), mentoring_schedules(mentor_name, date, time)').limit(1); console.log(JSON.stringify(error, null, 2)); } test();